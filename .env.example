# DynamoDB Viewer - Environment Configuration
# Copy this file to .env and adjust values as needed

# DynamoDB Local Configuration
# Set to 'true' to use local DynamoDB, 'false' to use AWS Cloud
DYNAMODB_LOCAL=true

# Local DynamoDB endpoint (default is localhost:8000)
DYNAMODB_ENDPOINT=http://localhost:9000

# AWS Region (used for both local and cloud)
DYNAMODB_REGION=us-east-1

# AWS Credentials (for local DynamoDB, any values work)
DYNAMODB_ACCESS_KEY=local
DYNAMODB_SECRET_KEY=local
